{
  "name": "@recon-struct/strix",
  "version": "1.0.1",
  "module": "./src/index.ts",
  "devDependencies": {
    "@recon-struct/utility-types": "^1.3.4",
    "@recon-struct/utils": "^1.0.8",
    "@types/bun": "^1.1.2",
    "rimraf": "^5.0.7",
    "typedoc": "^0.25.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "generate-docs": "typedoc --out docs src",
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist && tsc"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ]
}